{
  "name": "atc-ai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "AI Tender Compliance worker (FastAPI) — user's uploaded Python AI worker, run via venv",
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install fastapi 'uvicorn[standard]' pydantic requests python-multipart python-dotenv google-generativeai",
    "dev": ".venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 3010 --reload",
    "start": ".venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 3010"
  }
}
